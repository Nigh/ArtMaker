self.onmessage = async (event: MessageEvent<{bitmap:ImageBitmap;width:number;height:number;background:string|null}>) => { const {bitmap,width,height,background}=event.data; const canvas=new OffscreenCanvas(width,height),ctx=canvas.getContext("2d")!; if(background){ctx.fillStyle=background;ctx.fillRect(0,0,width,height);} ctx.drawImage(bitmap,0,0,width,height); const blob=await canvas.convertToBlob({type:"image/png"}); self.postMessage(blob); };
